{
  "name": "frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "icons": "node icons/generator.js",
    "dev": "nuxt",
    "build": "npm run icons && nuxt build",
    "start": "nuxt start",
    "generate": "npm run icons && nuxt generate --target static"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.12.0",
    "@nuxtjs/separate-env": "^0.0.1",
    "nuxt": "^2.14.0",
    "postcss-cssnext": "^3.1.0",
    "postcss-custom-media": "^7.0.8",
    "postcss-preset-env": "^6.7.0",
    "purecss": "^2.0.3"
  },
  "devDependencies": {}
}
